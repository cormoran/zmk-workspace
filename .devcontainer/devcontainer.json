{
  "name": "ZMK Development with west workspace",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "containerEnv": {
    "WORKSPACE_DIR": "${containerWorkspaceFolder}",
    "PROMPT_COMMAND": "history -a",
    // Required to share /root with other containers
    "Zephyr_DIR": "${containerWorkspaceFolder}/zephyr/share/zephyr-package/cmake/"
  },
  "mounts": [
    // The same to ZMK's official devcontainer
    "type=volume,source=zmk-root-user,target=/root"
  ],
  // NOTE: placed in runArgs mounts does not support exec option
  "runArgs": ["--tmpfs", "/${containerWorkspaceFolder}/build:exec"],
  "onCreateCommand": "bash .devcontainer/init.sh",
  "customizations": {
    "vscode": {
      "extensions": ["ms-vscode.cpptools"],
      "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
      }
    }
  }
}
